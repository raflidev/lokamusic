{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "miniplayer",
  "description": "Mini player floating window permissions",
  "windows": ["miniplayer"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close"
  ]
}
